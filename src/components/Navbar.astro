---
const links = [
  { href: "#inicio", label: "Inicio" },
  { href: "#empresa", label: "Empresa" },
  { href: "#componentes", label: "Componentes" },
  { href: "#contacto", label: "Contacto" },
];
---

<header class="bg-gradient-to-r from-slate-900 via-slate-800 to-cyan-900 text-white">
  <nav class="mx-auto max-w-7xl px-4 py-4 flex items-center justify-between">
    <a href="/" class="flex items-center gap-3 group">
  <img src="/Axioma_LOGO.png" alt="Axioma logo" class="h-10 w-auto transition-transform duration-300 group-hover:scale-110" />
  <span class="font-bold text-cyan-400 text-2xl tracking-wide group-hover:text-cyan-300 transition-colors duration-300">Axioma</span>
</a>


    <ul class="flex items-center gap-6 text-sm">
      {links.map(({ href, label }) => (
        <li>
          <a
            href={href}
            class="text-slate-200 hover:text-cyan-300 transition-colors"
          >
            {label}
          </a>
        </li>
      ))}
    </ul>
  </nav>
</header>